# my-code
up up
#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main()
{
		int n;
		scanf_s("%d", &n);
		int num=1;
		do
		{
			num *= n;	
			n--;
		} while (1!= n);
		printf("%d", num);
	return 0;
}
