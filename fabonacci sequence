# my-code
#include <stdio.h>

int Fibonacci(int x)
{
    int a = 1;
    int b = 1;
    int c = 0;
    while (x>2)
    {
        c = a + b;
        a = b;
        b = c;
        x--;
    }
    return c;
}

int main()
{ 
    int n = 0;
    scanf("%d", &n);
    printf("%d",  Fibonacci(n));
     
}
