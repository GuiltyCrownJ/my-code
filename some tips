# my-code
#include <stdio.h>
void Add(int* p)
{
	*p += 1;  
}

int main()
{
	int num = 0;

	Add(&num);
	printf("%d", num);
	Add(&num);
	printf("%d", num);
	Add(&num);
	printf("%d\n", num);
    printf("%d", printf("%d", printf("%d", 56)));


}
