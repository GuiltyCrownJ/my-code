# my-code
#include <stdio.h>
#include <string.h>

int factorial(int x)
{
    if (x > 1)
        return x * factorial(x-1);   // 4 3 2 1
    else
        return 1;
}

int main()
{ 
    int n = 0;
    int b = 0;
    scanf("%d", &n);
    b = factorial(n);
    printf("%d", b);
     
}
