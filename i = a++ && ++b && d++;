#include<stdio.h>
int main()
{
    int i = 0, a = 0, b = 2, c = 3, d = 4;
    i = a++ && ++b && d++;
    a = 1;
    b = 2;
    c = 3;
    d = 4;
    printf("a=%d\nb=%d\nc=%d\nd=%d", a, b, c, d);
  // i=a++ || ++b || d++;
  // a=1;
  // b=3;
  // c=3;
  // d=5;
    return 0;
}
