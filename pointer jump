#include<stdio.h>

// integer pointer jump a integer
// chart pointer jump a chart
// array pointer jump a array

int main()
{
	int arr[10] = { 0 };
	int* p = arr;
	int i = 0;
	for (i = 0; i < 10; i++)
	{
		*(p + i) = 1;
	}


	return 0;

	
}
  
